<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Profil</title>
    </head>
    <body>
		{{ user.username }}
		{{ user.mail }}
		{{ user.password }}
		{{ user.domain }}
        {% for annonce in annonces %}
            <h1><a href="/annonce/read/{{ annonce.id }}">{{ annonce.title }}</a></h1>
            <p>{{ annonce.description }}</p>    
        {% endfor %}
    </body>
</html>